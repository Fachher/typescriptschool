namespace basictypes {

    // number
    let n1: number = 1;

    // string
    let s1: string = '1';

    // symbol
    let sy1: symbol = Symbol('t');

    // tuples
    let t1: [string, string] = ['name', 'fachher'];

    console.log(sy1);
}
